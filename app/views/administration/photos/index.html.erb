<h1>Listing administration_photos</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @administration_photos.each do |administration_photo| %>
  <tr>
    <td><%= link_to 'Show', administration_photo %></td>
    <td><%= link_to 'Edit', edit_administration_photo_path(administration_photo) %></td>
    <td><%= link_to 'Destroy', administration_photo, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Photo', new_administration_photo_path %>
